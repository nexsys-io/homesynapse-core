# dashboard — Preact SPA — V1 observability dashboard, static files served from Javalin at /dashboard/, <100 KB gzipped initial bundle, separate npm/Vite build pipeline

*2026-07-27 beat (DASH-SERVE, core lane — the packaging model): this module is now a REAL resources-only jar (B-2 closed). `build.gradle.kts` applies bare `java` (deliberately not the convention plugin — no Java sources, no toolchain, no spotless surface); staging moved OUT of the source tree to `build/staged-dashboard` (with the `java` plugin, anything under `src/main/resources` would ride into every jar via `processResources` nondeterministically AND drag npm onto test classpaths — this is WHY staging lives under `build/`); the npm pipeline attaches to the JAR TASK ONLY (`assemble → jar → stageDashboard → npmBuild → npmInstall`), and the jar SELF-ASSERTS (doLast) that `dashboard/index.html` is inside the artifact. The app's existing `runtimeOnly(project(":web-ui:dashboard"))` carries the jar onto the runtime classpath; the app-side `testRuntimeClasspath` `exclude(module = "dashboard")` (DP-2) keeps `./gradlew check` Node-free — the DECOUPLING NOTE law now has TWO halves (jar-only attach + the app-side exclusion). Javalin serves the jar's `/dashboard` entries at `/dashboard/` with the SPA fallback + a `/` → `/dashboard/` 302 redirect (lifecycle composition root), behind the RULED posture-(A) GET/HEAD static-shell auth exemption (`api/rest-api` `RestFilters.isPublicShellRequest`). `install-smoke.yml` (both copies) gained `setup-node@v4` Node 22 (installDist now builds the SPA) and `run-smoke.sh` asserts the serve path headerless (`/` → 302, `/dashboard/` → 200) on every push. `clean` also sweeps the pre-DASH-SERVE legacy staging dir `src/main/resources/dashboard`. NO frontend-source/vite change rode this WU (P7: `base: '/dashboard/'` was already correct).*

*Frontend-dev lane, first beat delivered 2026-06-26. Was scaffold-only; now a buildable TypeScript Preact SPA (shell + design system + auth + device/health/event views + the explainability hero, built against the frozen read-API contract).*

*2026-06-29 beat (FE-0/FE-2/FE-3, per the master-plan amendment-1): design tokens are now **generated** from a platform-neutral W3C-DTCG source (D-FE-8); **dark is the recorded default** theme with a dark/light/system toggle honoring `prefers-color-scheme` (D-FE-1); copy is **name-light + i18n-keyed** (D-FE-9). D-FE-10 typeface RULED + landed: self-hosted subset Inter-variable (~25 KB woff2), system stack fallback. See Gotchas.*

*2026-06-30 beat (pre-hardware sprint T1.2/T1.3 + a11y): the mock is now a **scenario engine** — every contract condition (all outcomes/origins/verdicts/run-statuses, cascades, 300/500-row lists, empty) + every transport condition (503-replaying / offline / slow / 401 / 403 / ETag-304) one click away via a dev-only **DevPanel** + `?scenario=&mock=` URL params; **offline is now a first-class honest state**; an **axe-core** a11y suite gates WCAG structurally; and **`FE1_GO_LIVE.md`** is the live-integration swap checklist (realTransport audited against contract §0). See Gotchas.*

*2026-07-02 beat (FE-1 live integration + the E5 fold): the dashboard ran against a LIVE local Core (M7.5a/b endpoints, AB-1 bearer auth) for the first time. The **measured AMD-97 confirmation-rendering semantics are folded** — reason-aware honest-outcome copy + class-keyed pending/unconfirmable hints in `format.ts`/`CausalChain.tsx` (the UI runs NO confirmation timeout; the backend owns the per-capability window), plus an **`e5-confirmation` scenario** whose color-temperature run flips Sent→Confirmed live at the measured ~8.4s mark (`e5-semantics.test.ts` locks all four behaviors). FE-1 wiring: dev-server **proxy** `/api`+`/internal`→`VITE_CORE_ORIGIN` (default loopback:7070); **`VITE_VALIDATE=true`** dev-runtime contract validation in `client.ts` (console.error `[contract-drift]`, log-and-continue); strict `Accept: application/json, application/problem+json`; **EventsView renders an honest "hub doesn't share the activity feed yet" state on live 404** (the M7.5c gap — never an error); the C8 additive optional entity `name` folded into `contract.ts`/`shapes.ts` with `displayName()` preference; AuthGate asks password managers to ignore the pairing-token field (observed live: Bitwarden offered to fill it).*

*2026-07-02 beat 2 (FE-1b, the v1.1.1 fold): the two adjudicated FE-1 drifts are folded client-side against freeze **v1.1.1**. **DRIFT-2:** problem `type` is the URI form (`https://homesynapse.local/problems/<slug>`, Doc 09 §3.8) — `contract.ts` pins `PROBLEM_TYPE_URI_PREFIX` + `problemSlug()`; `ApiProblem` gains a derived `.slug` and ALL type-keyed detection (auth/forbidden/replaying/offline/EventsView not-found) keys on the slug suffix, tolerating bare slugs for client-minted problems; the mock emits URI-form types so mock === wire. **DRIFT-1:** A4/A5 are enveloped-real since core `e3d7296` (M7.5c-a) — no client shim ever existed (the envelope check was always strict); the fold is contract-currency: A4 gains the ruled additive `entityCount?`/`ready?`; A5 `parkedSubscribers` corrected to the RATIFIED `string[]` (ids; the mirror's pre-ratification object guess is removed — HealthView renders ids + the additive `subscribers[]` detail when present). `contract.test.ts` pins the prefix, the slug rule, the ratified A4/A5 shapes, and that bare bodies FAIL; `contract-check.mjs` trips on version + prefix drift. CONTRACT_VERSION = `v1.1.1-2026-07-02`.*

*2026-07-19 beat (explainability-UX lane — the verdict surface): the TEN-value `command_result.outcome` vocabulary (CommandResultEvent.java:22-27) gets a dedicated honest-rendering layer, **`src/lib/verdicts.ts`** (+ `verdicts.test.ts`): all ten outcomes distinctly labeled per the charter classes (honest-can't-know / known-failed / deliberately-superseded / restart-accounting / protocol-ack); superseded + expired_on_restart are recovered from the FLATTENED causal-chain FAILED via the ledger's deterministic recorded-reason strings ONLY (never guessed from variable text) and render calm, never as failure. Field-evidence classes rendered honestly end-to-end: the SILENT-SKIP do-nothing run (actionCount>0 / commandCount=0 / empty actions[] → never clean success, `CausalChain`/`causalSentence`), the prior-instance NULL-NAME run (`runName`/`NULL_NAME_NOTE`; contract mirror + validators tolerate exactly-null `automationName`/`trigger.type` per the OBSERVED live wire — clarification ask routed to the hub), availability as EVIDENCE-WITH-AGE (`availabilityEvidence` — flag + last-heard-from age; honest UNKNOWN-after-restart calm), DP-B2 "ran fine" disambiguation in WhyNotView (NEVER_TRIGGERED + non-null lastRelevantRunId → "It did run", linking to the run page for the do-nothing truth), and brightness percent from the DERIVED `brightness_percent` key only (`brightnessDisplay` — never a client 0–254 rescale). Three new scenarios: `verdict-vocabulary` (wire-faithful flattened reasons), `field-evidence`, `live-fleet` (canonical keys incl. temperature_c/humidity_pct + brightness level+percent). Confirmation POSTURE vocabulary (best_effort etc.) is design-ready in verdicts.ts but does NOT flow through v1.1 — recorded contract gap. See `nexsys-hivemind/context/audits/2026-07-19_explainability-ux-lane_return.md`.*

*2026-07-26 beat (FE-VERDICT-2 — the honest-verdict rendering lane): the **ruled v1.1.2 contract deltas are consumed** (ratified 2026-07-22 Nick ruling 1; landed core-side 2026-07-26, WU-SKIP-VIS, DP-4 GO; **MOCK-pending-deploy** — the deployed Pi read surface predates the landing until the deploy evening completes, so the mirror marks the three additive keys OPTIONAL: absence = lawful pre-v1.1.2 payload, presence validated strictly). **THE FIVE HONEST FAILURE MODES RENDER DISTINCT** (Nick's 2026-07-25 law — "the distinction IS the product"): `verdicts.actionVerdict()` classifies each causal-chain action from `(outcome, resultOutcome, reason)` into timed-out / superseded / acked-silent / held-DISPATCHED / settled-FAILED, each with its own LABEL + GLYPH (`MODE_GLYPHS`; StatusPill accepts per-verdict glyph + `provisional` variant) — never hue alone (§3a CVD mandate). **D-4 RETIRED:** the first-class `resultOutcome` wins where present; `classifyRecordedReason` recovery survives ONLY for pre-v1.1.2 payloads (the `verdict-vocabulary` scenario keeps that path exercised; the ruled wire is the new `five-modes` scenario). **Provisional/settled (§5.9):** `isActionSettled()` = the first-class `settled` field, else the client-side derivation IDENTICAL to the core Q1b formula; unsettled actions render dashed + "Sent — not settled yet" and the terminal line carries "— one outcome has not settled yet". **noCommandsIssued consumed** (WhyNotView: "Ran, but sent nothing" + link; the field-evidence scenario now carries the ruled ACTED_BUT_UNCONFIRMED + marker truth). **G2:** the Overview availability tile renders the honest states (Available-as-last-concluded / Offline / Not-determined-yet / Stale + the "not a live connection test" line); availability pills carry the honesty `help` (availabilityMeta). **AA mode-paired token corrections** (computed this session): light `ok500` #1a8f4c→#157a40, light `warn500` #9a6700→#7f5500, light `info500` accent.500→accent.600, dark `textMuted` neutral.500→#7e8998 — the light-theme pill pairs previously read 3.65–4.33:1 at pill size (the same light-mode failure class the brand program found). CONTRACT_VERSION = `v1.1.2-2026-07-26`. See `nexsys-hivemind/context/audits/2026-07-26_FE-VERDICT-2_lane_return.md`.*

*2026-09-06 beat (FE-113 — the v1.1.3 mirror, the CG-123 fast-follow): the **four additive keys** CG-1/2/3 landed core-side at `f25291b` (2026-09-06; the SKIP-VIS shape) are mirrored in the dashboard: `CONTRACT_VERSION` → `v1.1.3-2026-09-06` (`contract.ts`, its test pin, and `scripts/contract-check.mjs`'s `EXPECTED_VERSION` — the three move together); `contract.ts` declares `EntitySummary.deviceId?: string|null` · `EntitySummary.lastReported?: string|null` · `NonFiringExplanation.triggerRef?: SubjectRef|null` · a named `ComponentSummary` with `ref?: SubjectRef|null` (additive; `AutomationSummary.components` keeps its shape); `shapes.ts` validates the **TRI-STATE** per key (absent passes · null passes · a present key must be typed; `refOrNull` beside `strOrNull`); the honesty tests live in `src/lib/api/v113-additive.test.ts`. **Rendering:** the device list's freshness cell splits into three renders (key ABSENT → em-dash + `LIST_FRESHNESS_NO_CLAIM_TITLE`; PRESENT-null → em-dash + `LIST_FRESHNESS_NULL_TITLE`; PRESENT-string → `lastReportedCell`), plus a muted `Device <ulid>` line ONLY when the wire carried a string; WhyNotView renders `triggerRef` and AutomationsView renders `components[].ref` THROUGH the FE-HONEST-1 registry census (`lib/registry.ts` `useRefResolver` — resolved → the name; dangling on a complete census → the LOUD `StatusPill` + `UNRESOLVED_REF_PHRASE`, the causal chain's exact render; null / absent → the sentence alone). The default mock is a v1.1.3 hub that does NOT always populate (≥1 null per key; one dangling component ref, `DANGLING_LAMP_ULID`). The two recorded fixtures validate UNCHANGED (absence lawful). Copy is i18n-keyed (`devices.freshness.*`, `devices.deviceIdLabel`). Register: REPO-COMPLETE, LIVE-VERIFICATION PENDING (H8's real-wire read of the four keys). Return: `nexsys-hivemind/context/audits/2026-09-06_FE-113_return.md`.*

*2026-09-10 beat (FE-NULL-1 — the causal chain's null arms, the HERO-0 F2/F4 fast-follow on FE-113; core `39c8dd3`, NO version bump — the wire did not change, the mirror's TYPES were wrong about it): four B3 `/runs/{id}/causal-chain` keys the emitter serves as JSON null are now typed `| null`, validated null-or-typed, carried by the default mock on one null arm each, and rendered by the honest sentence HERO-0 wrote (`context/research/2026-09-06_HERO-0_null-census_v1.1.3_return.md` §1). **TWO IDIOMS, ONE TABLE:** the v1.1.2/.3 additive keys are OPTIONAL-NULLABLE (`key?: T|null`; `if ('key' in o)` — absent passes, the 2026-09-06 beat); these four are v1.1 BASE keys and therefore REQUIRED-NULLABLE (`key: T|null`; `strOrNull(req(...))` / `refOrNull(req(...))` — MISSING is a `ContractError`, null passes, a wrong type fails). The emitter lines of record: `trigger.subjectRef` null when the triggering event is outside the run's correlation (`StandardExplanationService.java:644–:649`, `.orElse(null)`; WHEN is undocumented — F3, a docs row) → "Something set it off at {time} — what isn't recorded." (`unrecordedTriggerLine`; the step keeps `matchedAt` + the type/firingValue detail; no label, no dangling pill — a null is not an unresolvable id); `conditions[].observedState[].value` null when the entity had no value at evaluation (`RunExplanation.java:137`) → "{entity} {attr} had no reading yet." in the "At the time" detail (`noReadingLine`/`NO_READING_YET`; every entry's `entityId`/`attribute`/`value` is now validated — previously nothing inside the array was checked); `actions[].command` null for a SKIPPED/FAILED action that never issued a command (`:776`) → "Skipped before any command was sent." (`SKIPPED_BEFORE_COMMAND`; the `Command` detail still says `not recorded`); `actions[].targetRef` null when the action had no target refs (`:771`) → the line ends "…a device the run didn't name." (`UNNAMED_TARGET`; **`targetRef` had NEVER been validated** — now required, null-or-`{type,id}`); **F4:** `cascade.parentRunId` is ALWAYS null in V1 (`RunExplanation.java:213–:219`) and a null is NOT "root" — `depth > 0` with no parent id now renders "Started by another run — which one isn't recorded." (`CASCADE_PARENT_UNRECORDED`, no link; depth 0 renders nothing as before). The headline (`causalSentence`) carries the same two fragments for a null trigger subject / null target. The mock: `run_eh_003` (the OLDEST run) carries `subjectRef: null` + the `:776` action (`command: null`, `targetRef: null`) — the H8 false type `command: 'turn_on'` on a SKIPPED action is gone; `run_fd_001` carries one `value: null` entry and `cascade: { parentRunId: null, depth: 1 }`. `scenarios.ts` is unchanged (its `makeChain`/`makeAction` factories still always populate — the arms reach every scenario through the `default` dataset only; a `null-arms` scenario is the next mock touch). Tests: `v113-additive.test.ts` (the FE-NULL-1 blocks, red-first register per row) + `CausalChain.hardening.test.tsx` (one render per arm + F4; `null` never as text). Register: REPO-COMPLETE, LIVE-VERIFICATION PENDING (the surface earns "verified" when a real-wire SKIPPED run renders the sentence — H8-a/R-4c's capture, FE-113b). Return: `nexsys-hivemind/context/audits/2026-09-10_FE-NULL-1_return.md`.*

## Design Doc Reference
- `homesynapse-core-docs/design/13-web-ui-observability-mvp.md` (Locked) — governs stack + UX scope.
- `nexsys-hivemind/context/decisions/2026-06-21_dashboard-read-API-contract-freeze.md` (FROZEN v1.1 base, amended ADDITIVELY — the freeze doc's own stamp reads **v1.1.2**, 2026-07-26; the **v1.1.3** amendment note is hub-owned and pending at the FE-113 landing, while the emitter (`f25291b`) and this mirror are already at v1.1.3) — the read-API contract this builds against; mirrored field-for-field in `src/lib/api/contract.ts`. v1.1.1 = the URI-form problem `type` (clients key on the slug suffix) + the A4/A5 envelope conformance ruling. v1.1.2 = the three additive keys (`actions[].resultOutcome`, `actions[].settled`, non-firing `noCommandsIssued`) + the DP-3 `triggeredAt`/`lastEvaluation.at` VALUE corrections. v1.1.3 = the four additive keys (`entities[].deviceId`, `entities[].lastReported`, non-firing `triggerRef`, `automations[].components[].ref`), every one PRESENT-or-null on a v1.1.3 hub.
- `FRONTEND_DOCTRINE.md` (this module) — the lean, reusable frontend doctrine (candidate for hub promotion).

## Dependencies
- Runtime: consumes the Core HTTP surface (REST) over loopback — A1–A5 real (A4/A5 enveloped since M7.5c-a) **and** the B3 hero four (runs, causal-chain, non-firing, automations) real since M7.5a/b; B1 `/events` + B2 `/health` still mocked to frozen shapes until M7.5c-b/c. No Java dependency; communicates only over HTTP.
- Build: Node + npm + Vite (dev/build only; not on the Pi at runtime). Preact 10, uPlot (lazy), TypeScript, Vitest, ESLint.

## Consumers
- The distribution / jlink image: Javalin serves the packaged SPA from the `:web-ui:dashboard` resources jar (classpath, entries under `/dashboard`) at `/dashboard/` (Doc 13 §3.2–§3.3; DASH-SERVE 2026-07-27). Gradle `:web-ui:dashboard:jar` (reached via the app's `installDist`/`assemble`) runs the npm build, stages `dist/` under `build/staged-dashboard`, and packages it into the jar under `/dashboard` with a doLast self-assert.

## Constraints (locked — do not re-litigate)
- **Stack:** Preact 10 + Vite + uPlot + CSS Modules + TypeScript (Doc 13, Locked). The dispatch's older "Svelte/React" framing is superseded.
- **Bundle budget:** 100 KB gzipped initial bundle, build-enforced hard fail (`scripts/check-bundle-size.mjs`). Current estimate ~22–26 KB.
- **No WebSocket in V1 (firm):** poll REST at 1–2s, coalesced on `meta.viewPosition` (`src/lib/poll.tsx`). Supersedes Doc 13's WebSocket-era sections (§1, §3.5, §3.8, §8.2, parts of §4/§6/§11) — flagged to the hub as a Doc 13 currency note.
- **Auth (AB-1):** in-memory session token only (no localStorage/cookies). Paste the pairing token from `config/initial_api_token`. Handle 401 → prompt, 403 → reject.
- **Local-first (C13-06):** no runtime CDNs, fonts-from-Google, or analytics. Everything ships in the image.
- **Write-isolation:** this lane writes ONLY under `web-ui/dashboard/…` + its lane return under `context/audits/`. Contract changes are cross-lane events routed to the hub.

## Gotchas
- **One switch flips mock/real:** `src/lib/api/index.ts` (`VITE_USE_MOCKS`; default mock in dev, real in prod). Endpoints are transport-agnostic; only the transport changes as Core lands B-class.
- **Contract drift fails CI, not the demo:** runtime validators in `src/lib/api/shapes.ts` + `contract.test.ts` + `scripts/contract-check.mjs`.
- **Problem types are URI-form on the wire (v1.1.1):** key detection on `ApiProblem.slug` (the suffix of `PROBLEM_TYPE_URI_PREFIX`), NEVER on the whole `type` byte-for-byte and never on a bare-slug equality against a wire value. Client-minted problems (`network-unreachable`) stay bare — `problemSlug()` tolerates both.
- **Plain-language copy is centralized** in `src/lib/format.ts` and locked by `format.test.ts` (the stranger/"mom" test).
- **Build/test require Node** and run as the frontend CI gate (`ci/frontend.yml`, npm `verify`). The Core lane's `./gradlew check` is intentionally NOT coupled to Node (DASH-SERVE: npm hangs off the JAR task only, never `check`; the app-side `testRuntimeClasspath` exclusion is the second half — see the 2026-07-27 beat above). `install-smoke.yml` DOES need Node (installDist builds the SPA).
- **CI wiring is a cross-lane item:** `ci/frontend.yml` is delivered here, ready for the hub to place into `.github/workflows/`.
- **Entity display names:** the frozen v1.1 contract carries an OPTIONAL `name` on A1/A2/A3 (additive C8, 2026-06-26). The mirror + validators fold it; `displayName()` (format.ts) prefers it and falls back to the humanized `entityId`. Core fills it with the config/M9 work — absence is normal today.
- **Design tokens are GENERATED — never hand-edit `tokens.css`.** Source of truth: `src/styles/tokens/tokens.dtcg.json` (W3C Design Tokens). Regenerate with `npm run tokens`; the drift-guard `npm run tokens:check` is wired into `verify` (and `prebuild` regenerates). The generator (`scripts/build-tokens.mjs`) is zero-dependency; the source is DTCG, so Style Dictionary can be adopted later for native/B2B outputs without re-authoring (D-FE-8).
- **Dark is the recorded default theme (D-FE-1).** First paint is set pre-CSS by an inline boot script in `index.html` (no FOUC); `prefers-color-scheme` is honored for "system"; a persistent dark/light/system toggle (`src/lib/theme.ts` + `src/components/ThemeToggle.tsx`) sets `[data-theme]` on `<html>`. The theme preference is stored in `localStorage` — this is non-secret UI state and is deliberately distinct from the in-memory-only auth token (AB-1).
- **Name-light + i18n-keyed copy (FE-3, D-FE-9).** The product name comes from `BRAND.productName` (`src/lib/i18n.ts`) — never hardcode it (the unratified rename flips one token). User-facing strings are a keyed catalog via `t()`; V1 ships English; locale/RTL/Intl-format seams are reserved. (Most enum-label copy still lives in `format.ts`; migrating those maps behind `t()` is the same pattern and can follow. The static `index.html` `<title>`/`<noscript>` remain as the no-JS fallback; `document.title` is set from the brand token at runtime.)
- **UI font is self-hosted (FE-3/D-FE-10), not a runtime web font.** `src/styles/fonts/inter-variable-subset.woff2` (~25 KB, Inter variable `wght` axis, tight UI subset) ships in the image and is served from loopback — **never a CDN** (INV-LF-01). Wired in `src/styles/fonts.css` (`@font-face`, `font-display: swap`); `--hs-font-sans` leads with `"Inter Variable"`, system stack as fallback. Reproducible subset: `src/styles/fonts/README.md`. (Same surface as the dashboard↔website type-coherence goal.)
- **The mock is a SCENARIO ENGINE (T1.2), not a single happy path.** `src/lib/api/mock/scenarios.ts` (contract-shaped datasets + factories) + `mockState.ts` (active scenario + transport condition, URL-driven) drive `mockTransport.ts`. The **real/mock swap is still ONE switch** (`index.ts`, `VITE_USE_MOCKS`) — scenarios only shape mock data. The **DevPanel** (`src/components/DevPanel.tsx`) renders **only when `API_MODE === 'mock'`** (absent in a real build) and sits outside the auth gate (so 401/403 are recoverable). `contract.test.ts` validates **every** scenario against `shapes.ts`. Add a scenario = add a `build()` to the registry; keep it contract-shaped.
- **Offline is a first-class honest state.** `client.ts` wraps a transport network failure as `ApiProblem` `network-unreachable` → `isOffline`; `poll.tsx` + `useApi` carry an `offline` phase/status; `feedback.tsx` `OfflineState` renders it calmly (never a generic error, never a fake success — INV-SA-03). Exercise it via `?mock=offline`.
- **a11y is machine-gated (axe-core).** `src/a11y.test.tsx` runs axe over the hero + toggle + auth + pills + feedback states inside `npm run test` (jsdom → color-contrast disabled there; verified visually). **Requires the devDep:** `npm install --save-dev axe-core@^4.10.2` (regenerates the lockfile), then `npm run verify`. `vitest.config.ts` sets esbuild's automatic Preact JSX runtime so component tests can render `.tsx` (the Vite plugin's type conflicts with vitest's bundled Vite).
- **FE-1 readiness:** `FE1_GO_LIVE.md` — the realTransport audit against the frozen contract §0 + the one-sitting go-live checklist. Two follow-ups noted for FE-1 (cursor-follow for large sets; optional dev-runtime validation); neither is structural.
- **[NEW-2/3 fold, 2026-08-17 — hub-owned per the F-14-row precedent; source: the FE-lane return §7 dev-7, landed at `beb664e`] The ErrorBoundary is mounted APP-WIDE at the view switch** (`app.tsx`, full-route-identity `resetKey`): every view renders contained; inner per-view boundaries are optional refinements on top, never the containment mechanism. A raw uncaught render `TypeError` anywhere is a NEW-2 regression by construction (contained failures log `[render-error] contained by ErrorBoundary:` and render the honest failure card).
- **`lastEvaluation` is OBJECT-OR-NULL per the observed wire** (`contract.ts:361`; the 2026-08-16 200 OK non-firing body is on record verbatim as `src/lib/api/fixtures/wire-2026-08-16-nonfiring.ts`, provenance-labeled). The mock fleet carries the real tri-state (`makeNonFiring` non-CONDITION_NOT_MET arms serve `null`) — a mock that always populates a nullable field MANUFACTURES the false type that caused the 2026-07-27/2026-08-16 incident class (H8).
- **`parseInstant`/`clockTimeWithDate` (`src/lib/format.ts`) are the ONLY lawful instant renderers** — never coerce a non-string instant (the seconds-as-ms 1970-misread class is closed; honest em-dash instead); stamps past 24 h are date-qualified; ONE shared parse feeds device-detail prose AND the Last-reported row (the DX-20 self-contradiction dead at the class).
- **[FE fixture/F-S3 fold, 2026-08-21 — hub-owned per the F-14-row precedent; source: the FE-lane return `nexsys-hivemind/context/audits/2026-08-21_FE-lane_never-triggered-fixture_F-S3_return.md` §5–§7, hub-audited at the diff] The non-firing wire has TWO provenance-grade captures across two deployments** (`src/lib/api/fixtures/wire-2026-08-16-nonfiring.ts`, gate-day build; `wire-2026-08-20-never-triggered.ts`, the NEW-2/3 build `index-C95CAnmp.js` at `c091f7c` — both byte-complete: 395 B / 396 B compact), and **`src/lib/api/fixtures/fixtures.stability.test.ts` is the cross-deployment DRIFT DETECTOR** (identical key sets recursive incl. envelope · identical null-ness · the value-diff set EXACTLY `{data.automationId, meta.viewPosition, meta.timestamp}` · key-order + byte-size pins separately named; seven teeth tests on mutated copies). A third capture EXTENDS the detector with one import — it never adds an "arm" (the `lastEvaluation` tri-state is absent/null/value; two real captures of the null arm are stability evidence, not a third arm). **Known blind spot:** `walk()` does not descend ARRAYS — two captures that both carry an array at the same path with different contents diff SILENT; before reusing the detector on `/state` (FE-STATE-DIALECT) descend arrays by index or compare an array leaf via `JSON.stringify`.
- **The run-detail `Updated` stamp ages BY CONSTRUCTION — and only there.** `useApi` (`poll.tsx`) refetches on every `viewPosition` change, but the causal-chain read is the ONE surface with a STRONG, content-constant ETag (`"<runId>"`, `GetRunCausalChainEndpoint.java:100–:102`) → Javalin answers `304` → the client's 304 path returns its CACHED result, `meta.timestamp` included (`client.ts` 304 path) → `Freshness` re-renders the FIRST response's generation time against a fresh `Date.now()`. List views carry weak position-keyed ETags (`W/"<viewPosition>"`) that change with the cursor → `200` → the stamp resets; `ListEntitiesEndpoint` sets no ETag at all. This is the F-S3 observation (sitting record 2026-08-20 §3), not a defect; the ruled copy change (`As of <time> · live`, the poll phase word from the footer; tooltip carries the mechanism; the 304 path refreshes the cursor from `X-HomeSynapse-View-Position`) rides the next FE touch — the FE-STATE-DIALECT WU — not a standalone lane. The contract is silent on ETag keying; do not assume strong ETags anywhere else.
- **The Activity teaching state is keyed on the WIRE DISCRIMINATOR, never on a problem slug:** `client.ts` mints `endpoint-not-in-this-release` when (path ∈ `FROZEN_UNBUILT_PATHS` — today ONLY `/api/v1/events`, exact match) ∧ status 404 ∧ media type `application/json` (parameters stripped), decided from headers without consulting the body (the headers-only fixture `wire-2026-08-20-events-404-headers.ts` is labeled HEADERS-ON-RECORD, BODY NOT CAPTURED); `EventsView` keys the shipped `events.notServedYet.*` card on `ApiProblem.isUnservedEndpoint`. The `not-found`-slug arm is RETIRED — a Core-authored problem+json `not-found` on that path renders the generic honest card. **THE PATH SET IS A LAW: it MUST SHRINK in the same change that swaps `/api/v1/events` mock→real** — a stale entry turns the teaching card into a lie the moment Core routes the path. Surface status: REPO-COMPLETE, LIVE-VERIFICATION PENDING THE DEPLOY (H8) — the re-exercise plan is the return's §8. A cheap unit pin not yet present: assert `api.listEvents` sends exactly `/api/v1/events` (the discriminator never fires live if the path literal drifts).
- **[FE-113, 2026-09-06] THE TRI-STATE IDIOM — absent ≠ null ≠ value, on every additive key.** A v1.1.3 hub serves every new key PRESENT (JSON `null` when it has nothing on record — never absent); a pre-v1.1.3 hub omits the key entirely. These are TWO facts and they render as TWO sentences (the device list: `LIST_FRESHNESS_NO_CLAIM_TITLE` for the absent key, `LIST_FRESHNESS_NULL_TITLE` for the present-null one). The validator law that makes "optional" impossible to abuse: `if ('key' in o) <typed-or-null check>` — absent passes, null passes, a present wrong type is `ContractError` (a number where `lastReported` should be an ISO string is the epoch-seconds class; a ref without `id` is drift). The C8 `name` precedent is the ABSENCE idiom; `noCommandsIssued`'s validator is the PRESENT-must-be-typed idiom; `refOrNull`/`strOrNull` are the shared helpers. Corollary at the mock: every entity row, every non-firing entry and every component carries the key, and at least one carries `null` (a mock that always populates hides the null — H8). `deviceId` is read from the LIVE registry at request time while the row's other fields come from the state snapshot at `viewPosition` — two instants; a null is honest, never a defect (the hub's freeze-doc note carries the consistency window).
- **[FE-113, 2026-09-06 — the CG-123 §DP-5 reading, recorded as a GOTCHA, not a code change] `outcome: CONFIRMED` beside `resultOutcome: null` on a causal-chain action IS THE HAPPY PATH.** `state_confirmed ⇒ CONFIRMED` regardless of any `command_result`, and `resultOutcome` is a pure fact-carry of the LAST `command_result` chained to the `command_issued` — which the Zigbee handler publishes ONLY for `unroutable` / `rejected` / `unconfirmed`, never on success. So "confirmed by the device's own report; the adapter published no verdict" lawfully reads `CONFIRMED` + `null`: not a defect, not a join miss, never to be rendered as "outcome not recorded" or as provisional. (`verdicts.actionVerdict` renders CONFIRMED as settled "Confirmed" whatever `resultOutcome` carries — `verdicts.ts:368`; the `live-nulls` scenario carries the OBSERVED `CONFIRMED` + `resultOutcome: null` + `settled: true` arm. NOTE, filed in the FE-113 return and NOT changed here: the `default` scenario's two CONFIRMED actions and `scenarios.ts:75` carry `resultOutcome: 'acknowledged'` — per §DP-5 the live happy path serves `null` there, so that value is a mock fiction of the H8 false-value class; the next mock touch should null it.) `trigger.firingValue: null` on a matched trigger is likewise V1-by-design (no payload-type extraction rule yet — FIRING-VALUE, hub-docketed); render "value not recorded", never a claim.
- **Core-side observation carried here for the consumer (no FE patch — the contract-consumer law):** endpoint-level problems reach the wire as `application/json` (`EndpointResponses.problem()` → `ctx.json()`), while only the exception path (`RestFilters.java:551`) sends `application/problem+json`; `contract.ts:58` + Doc 09 §3.8 say non-2xx bodies are problem+json. `toProblem()` keys on the BODY's `type` slug, so nothing breaks; the Core fix (F-V2) is a one-liner routed with the R-6/R-8 WU.

---

## Phase 3 Cross-Module Context

*Added 2026-04-11 (Alignment Pass #2); the "scaffold only" note below is now superseded by the 2026-06-26 first-beat delivery above.*

**Phase 3 cross-module decisions register:** `nexsys-hivemind/context/decisions/phase-3-cross-module-decisions.md` is the running list of decisions made during Phase 3 implementation that cross module boundaries.

**Read also:** `nexsys-hivemind/context/status/PROJECT_SNAPSHOT.md` for current milestone state.
